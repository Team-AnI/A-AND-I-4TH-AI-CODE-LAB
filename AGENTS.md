# AI-code-lab 실습 자료 제작 하네스

> 이 문서는 **강의 실습 자료를 제작하는 AI 에이전트가 따라야 할 규격서**입니다.
> 대화 이력이 없어도 이 문서 + 해당 주차의 강의자료만 있으면 **동일한 품질·구조의 산출물**이 나와야 합니다.

---

## 0. 에이전트 동작 개요 (TL;DR)

입력받는 것:
1. **주차 번호**와 **주제** (예: "4주차 — MovieLens 협업 필터링")
2. **강의자료** (개념 설명, 실습 스펙, 학습 목표 등 — PDF/슬라이드 등으로 **별도 제공**)

출력해야 할 것 (해당 주차 폴더 `N주차/` 안에):
1. `lab_0X_<slug>.ipynb` — 실습 노트북 여러 개 (Colab 실행 가능). **설명·코드 해설을 노트북 안에 내장**.
2. `README.md` — 주차 전체 개요 (하나만)

> **핵심:** lab별 별도 `.md` 파일은 만들지 않습니다. 설명은 노트북 안의 마크다운 셀("🔬 코드 해설")에 직접 넣고, 주차 단위 안내만 `README.md`에 둡니다.

실습이 여러 개면 `lab_01`, `lab_02`, ... 로 넘버링합니다.

---

## 1. 전체 커리큘럼

| 순서 | 주제 | 핵심 키워드 |
|------|------|-------------|
| 1 | 오리엔테이션 | 학습 목표·도구 소개 |
| 2 | PyTorch | Tensor, Autograd, Training Loop |
| 3 | MNIST — EDA, 전처리, 특징 공학 | Dataset, DataLoader, Feature Engineering |
| 4 | MovieLens — Collaborative Filtering | Embedding, Cosine Similarity, User/Item Matrix, MF |
| 5 | MovieLens — SASRec | Attention, Transformer, Sequential Recommendation |
| 6 | Airflow | DAG, Scheduling, Operators, Pipeline, Orchestration |
| 7 | 논문 조사 및 발표 | Paper Research, Presentation |
| 8 | 기초 프로젝트 | End-to-End 구현 |
| 9 | 팀 프로젝트 | 협업, 배포 |

주차 간 **연속성**을 유지하세요. 이전 주차/이전 lab 개념을 새 자료에서 짧게 상기시키고, 다음 주차와의 다리도 마지막에 둡니다.

---

## 2. 디렉터리 및 파일 규칙

```
AI-code-lab/
├── AGENTS.md                      ← 현재 파일 (수정 금지)
├── 4주차/
│   ├── README.md
│   ├── lab_01_pipeline.ipynb
│   ├── lab_02_explain.ipynb
│   ├── lab_03_apply.ipynb
│   └── [강의자료] *.pdf            ← 별도 제공
└── N주차/
    └── ...
```

### 파일명 규칙
- **주차 폴더**: `N주차/` (한글 "주차" 포함)
- **노트북**: `lab_0X_<slug>.ipynb` — 영어 slug, 소문자, 단어 사이 `_`
  - slug는 **학습 단계 역할**을 드러내는 게 표준입니다: `pipeline`(따라하기) → `explain`(전체 설명) → `apply`(확장)
  - 주제 성격상 단계 아크가 맞지 않으면 내용 기반 slug(예: `eda`, `training_loop`)도 허용
- **README**: 주차 폴더 안에 하나만
- **lab별 `.md` 파일은 만들지 않습니다.**

---

## 3. 교수법 철학 — "해설 중심 3단계 아크"

이 실습의 근간은 **"사고력 설계자(Cognitive Designer)"** 모델입니다. 지식을 한 번에 떠먹이지 않고, 수강생이 **실행 → 분해 → 응용**의 단계를 밟으며 **스스로 구성**하도록 설계합니다.

### 금지되는 태도
- "걱정 마세요, 다 설명해드릴게요" 식 전지적 어조
- 변수명만 봐도 아는 내용을 반복하는 해설
- "이 코드는 무엇을 한다"만 나열하는 설명 (WHAT만 있고 WHY가 없음)

### 지향하는 태도
- "어려운 건 당연합니다. 한 줄씩 왜 그런지 짚으며 스스로 이해하도록 돕겠습니다."
- 코드 해설은 **WHY 중심**: 왜 이렇게 작성했는지, 어떤 원리가 깔려 있는지
- 수강생이 막혔을 때 정답을 베끼기 전에 **자기 언어로 먼저 요약 → 검증**하도록 유도

### 🧭 3단계 학습 아크 (실습이 3개일 때의 표준)

| lab | 역할 | 특징 |
|-----|------|------|
| **lab_01 — 따라하기 (`pipeline`)** | "이렇게 된다" | 전체 파이프라인을 처음부터 끝까지 **한 번에 실행**. 코드 해설 없이 결과를 눈으로 확인 |
| **lab_02 — 전체 설명 (`explain`)** | "왜 그렇게 되나" | `lab_01`과 **동일한 코드**를 한 줄씩 분해. 코드 셀마다 `### 🔬 코드 해설` 셀을 붙여 원리를 설명 |
| **lab_03 — 확장 (`apply`)** | "직접 해본다" | EDA + 변형·구현. 앞 lab에서 배운 걸 응용해 새로운 구조를 직접 작성 |

실습 개수가 3개가 아니면 아크를 유연하게 조정하세요(2개면 따라하기+설명 통합 → 확장, 1개면 한 노트북 안에서 세 단계를 섹션으로).

---

## 4. 산출물 템플릿

> 아래 구조는 **표준**입니다. 주제에 따라 소절을 추가/변형해도 되지만, 명시된 **굵은 요소**는 반드시 포함하세요.

### 4-A. `lab_0X_<slug>.ipynb` — 실습 노트북 (공통 구조)

**셀 순서:**

1. **제목 셀 (Markdown)**
   - `# 실습 X: <제목> (<부제 한 줄>)` — 난이도 이모지/표기는 붙이지 않는다 (난이도는 강의에서 구두로 안내)
   - **목표** 한 단락 (이 lab이 아크에서 어느 단계인지 명시: 따라하기/전체 설명/확장)
   - **개념 복기 및 이론 점검** — 진행 전 스스로 답해볼 질문 2~3개 (정답 병기 금지)
2. **🚀 Open in Colab 배지 셀 (Markdown, 제목 셀 바로 다음)** — 아래 템플릿을 **그대로** 삽입. `<PATH>`만 해당 파일의 리포 상대 경로로 치환.
   ```markdown
   [![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/Team-AnI/A-AND-I-4TH-AI-CODE-LAB/blob/main/<PATH>)

   > 위 배지를 누르면 이 노트북이 **여러분 Google 계정의 Colab**에서 열립니다. 수정본을 남기려면 `파일 → Drive에 사본 저장`.
   ```
3. **번호 섹션들 (`## N. <소제목>`)** — 개념·단계별로 구분. 각 섹션은:
   - Markdown: `## N. 소제목` + 왜 이걸 하는지 1~2 문장
   - Code: 실행 가능한 짧은 코드
   - **전체 설명/확장 단계 노트북**: 각 코드 셀 **바로 다음**에 `### 🔬 코드 해설` 마크다운 셀을 붙여 원리를 설명 (따라하기 노트북은 해설 없이 실행 흐름만)
4. **시각화 셀** — 학습 과정·데이터·결과를 `matplotlib`로 그려 눈으로 확인 (특히 데이터셋 주차의 EDA)
5. **마지막 섹션 `## N. ✅ 학습 결과 정리 (Verification)`**
   - 이 lab에서 확인한 것들을 불릿으로 정리
   - 마지막 줄에 `🎯 **핵심 결론:**` 한 단락

**코드 작성 규칙:**
- Colab에서 **위에서 아래로 즉시 실행 가능**해야 함 (데이터는 표준 공개 소스: `torchvision`, MovieLens 등 — 첫 실행 시 자동 다운로드)
- 난수가 있으면 `torch.manual_seed(...)` 등으로 재현성 확보
- `device`가 필요하면 `cuda → mps → cpu` 순 자동 선택
- **주석은 최소화**. WHAT이 아니라 WHY가 비자명할 때만 한국어로 짧게. 변수명이 설명을 대신하게.
- 시각화는 한 figure당 최대 2~3개 subplot

**노트북 JSON 포맷:**
```json
{
  "cells": [...],
  "metadata": {
    "kernelspec": {"display_name": "Python 3", "language": "python", "name": "python3"},
    "language_info": {"name": "python", "version": "3.x"}
  },
  "nbformat": 4,
  "nbformat_minor": 5
}
```
각 셀: `{"cell_type": "markdown"|"code", "metadata": {}, "source": [...], "outputs": [], "execution_count": null}` (markdown은 outputs·execution_count 생략).

### 4-B. lab별 역할 상세 (3단계 아크 예시)

**lab_01 (`pipeline`) — 따라하기**
- 데이터 로딩부터 결과까지 전체를 한 번에 실행하는 "맛보기".
- 코드 해설 없이 섹션 제목 + 짧은 도입 + 코드 + 결과 출력만.
- 마지막 정리에서 "다음 lab에서 이걸 한 줄씩 뜯어본다"는 다리를 제공.

**lab_02 (`explain`) — 전체 설명**
- `lab_01`과 **동일한 코드**를 섹션별로 다시 배치하되, 각 코드 셀 뒤에 `### 🔬 코드 해설`.
- 제목 셀의 "개념 복기 및 이론 점검"에서 핵심 원리 질문을 던짐.
- 정규화·행렬곱·전치 같은 핵심 연산의 의미를 집중적으로 풀어줌.

**lab_03 (`apply`) — 확장**
- 보통 두 파트: **Part 1 EDA**(분포·희소성·롱테일 등 시각화) + **Part 2 직접 구현**(새 변형 구현).
- 앞 lab을 응용하므로 해설은 "왜 이 변형이 동작하는가"에 집중.
- 마지막 정리에서 주차 전체를 관통하는 `🎯 핵심 결론`.

### 4-C. `README.md` — 주차 전체 개요

**필수 섹션 (이 순서):**

~~~~markdown
# N주차 · <주제>

> <주차의 핵심 메시지 한 줄>

## 🎯 학습 목표
이번 주차가 끝나면 여러분은 다음 질문에 **자기 언어로** 답할 수 있게 됩니다.
1. ... (2~3개)

## 🧭 이번 주차의 lab 구조
```
[lab_01 따라하기] ──► [lab_02 전체 설명] ──► [lab_03 확장]
   "이렇게 된다"        "왜 그렇게 되나"        "직접 구현"
```
세 lab을 **순서대로** 보아야 의미가 통합니다.

## 📚 실습 구성
| # | 파일 | 역할 | 핵심 키워드 | 설명 | Colab |
|---|------|------|-------------|------|-------|
| 1 | [`lab_01_*.ipynb`](lab_01_*.ipynb) | 따라하기 — ... | `키워드` | 📖 노트북 내장 | [![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/Team-AnI/A-AND-I-4TH-AI-CODE-LAB/blob/main/N주차/lab_01_*.ipynb) |

> **Colab으로 실행하는 법:** 표의 **Open in Colab** 배지를 누르면 해당 노트북이 **여러분 Google 계정의 Colab**에서 열립니다. 실행·사용량은 각자 본인 계정 기준이며, 수정본을 남기려면 `파일 → Drive에 사본 저장` 하세요. (데이터는 첫 실행 시 자동 다운로드 / GPU가 필요하면 `런타임 → 런타임 유형 변경 → GPU`.)

## 🧭 권장 진행 순서
```
lab_01 (15분)   →   lab_02 (45분)   →   lab_03 (60분+)
"한 번 돌려본다"     "왜 그런지 이해한다"    "직접 짜본다"
```
(각 lab을 어떤 태도로 진행할지 1~2줄씩 안내)

## 🤖 이 실습을 AI와 함께 공부하는 법
- AI는 **답안지가 아니라 튜터**입니다.
- 막히면 정답을 베끼기 전에, 자기 언어로 먼저 요약하고 AI에게 검증받으세요.

## 🧩 이번 주차의 메타 질문 (파인만 기법용)
모든 실습이 끝나면 아래 질문에 **각각 3문장 이내**로 답해 보세요.
1. ... (비전공자/친구에게 설명하는 형태 3문제)

## 📦 요구사항
```bash
pip install ...
```
Colab에는 모두 기본 설치되어 있습니다.

## 📚 참고 문헌
- ... (논문/슬라이드, 가능하면 링크)
~~~~

---

## 5. 글쓰기 스타일 규칙

- **언어**: 한국어 (코드·변수명·영문 고유명사는 영어)
- **말투**: 존댓말 문어체로 일관. 학습자는 생략하여 지칭
- **톤**: 코치/멘토 톤. 단정적으로 답을 쏟아내기보다 "왜 그런지" 생각하게
- **분량** (4주차 기준 실측치):
  - `README.md`: 약 70~90줄
  - `lab_0X.ipynb`: 약 15~30개 셀이지만 초과 허용 (따라하기는 적게, 확장은 많게)
- **이모지**: 섹션 헤더 구분용으로만 사용. 남용 금지. 표준 팔레트: 🎯 🧭 📚 🔬 ✅ 🤖 🧩 📦 💡 ⚠️ 🔑
- **수식**: LaTeX 달러 표기(`$...$`, `$$...$$`) 사용.
- **링크**: md 내 파일 참조는 상대 경로 마크다운 링크
- **주석 (코드)**: 한국어로 짧게. WHAT이 아니라 WHY. 한 줄 원칙.

---

## 6. 실습 코드 작성 고려사항

각 주차의 실습은 **1, 2, 3**으로 구성됩니다. **1은 따라하기**(전체 파이프라인을 한 번에 실행), **2는 동일한 코드에서 모든 코드를 한 줄씩 설명**(🔬 코드 해설), **3은 확장된 실습**으로 주차별 주제에 맞게 다르게 표현됩니다.

| 유형 | 필수 요소 |
|------|----------|
| **이론·구현 주차** (2·5·6) | 강의자료(별도 제공)의 이론을 **직접 손으로 구현**하는 실습. 개념 검증 성격 |
| **데이터셋 주차** (3·4) | 실제 데이터 로딩·전처리·EDA 포함. **재현 가능한 seed**·자동 다운로드·필요 시 작은 서브셋 |
| **프로젝트 주차** (7~9) | 실습 lab 대신 템플릿·가이드·평가 루브릭 중심일 수 있음 — 이때는 구조를 유연하게 조정 |

**모든 주차 공통:**
- 지난 주차/이전 lab 개념을 1~2문장으로 상기
- 다음 주차와의 다리를 마지막에 제공 ("다음 주에는 ~ 를 배웁니다. 이번 주차의 X가 거기서 어떻게 쓰일지 예상해 보세요.")

> **강의자료에 대하여:** 강의 원문은 이 리포에 항상 들어있지 않고 **별도 제공**됩니다(PDF/슬라이드 등). 자료가 모호하거나 실습 개수가 불명확하면 임의로 선택하지 말고 사용자에게 되물어 확인하세요.

---

## 7. 에이전트 실행 체크리스트

새 주차 자료를 만들 때 에이전트가 순서대로 확인할 것:

- [ ] 사용자가 제공한 **강의자료**를 파싱해 **실습 개수**와 **각 실습의 목표·과제**를 추출
- [ ] 주차 폴더 `N주차/` 생성 확인 (없으면 생성)
- [ ] 실습을 **따라하기 → 전체 설명 → 확장** 아크에 매핑 (개수가 다르면 아크 조정)
- [ ] 각 `lab_0X_<slug>.ipynb` 생성 (**lab별 별도 `.md`는 만들지 않음**)
- [ ] 각 노트북 **제목 셀 바로 다음**에 Open in Colab 배지 셀이 있고, 배지 링크 경로가 실제 파일 위치와 일치하는지 확인
- [ ] 전체 설명/확장 노트북은 코드 셀마다 `### 🔬 코드 해설` 셀이 붙어 있는지 확인 (따라하기는 없음)
- [ ] 각 노트북 마지막에 `✅ 학습 결과 정리` + `🎯 핵심 결론`이 있는지 확인
- [ ] `README.md` 생성 — 학습목표 / lab 구조도 / **실습 구성 표(역할 + Colab 배지 컬럼)** / 권장 순서 / AI 공부법 / 메타 질문 / 요구사항 / 참고 문헌 포함
- [ ] 이전 주차·이전 lab 개념 상기 + 다음 주차 다리 포함 확인
- [ ] 노트북 JSON이 유효한지 (브라켓·쉼표) — `nbformat 4.5` 기준
- [ ] 노트북이 Colab에서 위→아래로 에러 없이 끝까지 실행되는 흐름인지 점검
- [ ] 최종 응답에서 생성한 파일 목록을 마크다운 링크로 정리

---

## 8. 사용자 입력 받는 표준 프롬프트 (참고)

사용자가 다음 주차 자료를 만들 때 이렇게 입력하면 됩니다:

```
AGENTS.md 규칙에 따라 N주차 실습 자료를 만들어줘.

[주차 번호]: N
[주제]: ...

[강의자료]:
<강의자료(PDF/슬라이드/요약 등)를 첨부하거나 붙여넣기 — 별도 제공>
```

에이전트는 이 문서의 규칙대로 노트북 + README를 생성합니다.
강의자료가 모호하거나 실습 개수가 불명확한 경우 임의로 선택하지 말고 다시 되물으며 명확하게 자료를 구성해야 합니다.

---

## 9. 품질 기준 (산출물 자가 검증)

산출물을 제출하기 전 스스로 확인:

1. **3단계 아크가 드러나는가?** — 따라하기에서 실행, 전체 설명에서 분해, 확장에서 응용의 흐름이 명확한가
2. **"🔬 코드 해설"이 WHAT이 아니라 WHY를 설명하는가?** — 변수명만 봐도 아는 내용을 반복하지 않는가
3. **이 주차의 개념을 읽기만 해도 흐름을 명확히 이해할 수 있는가?**
4. **코드는 Colab에서 위에서 아래로 실행했을 때 에러 없이 끝까지 돌아가는가?**
5. **README가 lab 구조·진행 순서·메타 질문을 모두 담아 수강생이 길을 잃지 않는가?**

이 5가지를 모두 만족하지 못하면 재작성하세요.

---

**🎓 최종 원칙:** "한 번 돌려보고(따라하기) → 왜 그런지 뜯어보고(전체 설명) → 직접 변형해 본다(확장). 설명은 답을 떠먹이는 게 아니라, 다음 단계를 스스로 밟게 만드는 발판이다."
